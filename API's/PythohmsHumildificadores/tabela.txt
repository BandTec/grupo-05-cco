create database pythohms;
use pythohms;


create table dados(
    id int primary key auto_increment,
    placaMae varchar(45),
    nCore int,
    mediaProcessamento decimal(6,1),
    temp decimal(4,2),
    memoriaUsada decimal(3,1)
);